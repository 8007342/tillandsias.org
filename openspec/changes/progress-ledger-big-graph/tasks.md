## 1. Source inventory

- [x] 1.1 Add a repeatable snapshot command for all canonical runtime specs and archived changes, including those without task records.
- [x] 1.2 Commit the numbered-release snapshot and make build reject pin drift.

## 2. Accountability ledger

- [x] 2.1 Keep every existing finding, including resolved entries, and expose its claim, evidence, remedy and history on expansion.
- [x] 2.2 Add complete spec and archived-change inventories; distinguish document lifecycle, checklist completion and verified behavior.
- [x] 2.3 Start all entries collapsed and provide search across rendered records.

## 3. Big Graph

- [x] 3.1 Add the fifth menu page and an onward link from slide 6.
- [x] 3.2 Map host, forge, filesystem, enclave, boundaries, credentials, certificates, lifecycle, remote and evidence flow for the website checkout example.
- [x] 3.3 Add bidirectional scrolling, fixed-type spacing control, progressive detail, layer toggles and a component inspector.
- [x] 3.4 Mark goal-only and partial components separately and link target claims to the pinned specs.

## 4. Verification

- [x] 4.1 Run checked build and ledger validation; inspect generated IDs and references and deterministic output.
- [x] 4.2 Exercise menu, search, collapse, graph controls and layer toggles in a DOM harness.
- [ ] 4.3 Review graph and ledger layout visually in a browser. Chromium download failed with `ECONNRESET` in this forge; the DOM check does not verify layout.
