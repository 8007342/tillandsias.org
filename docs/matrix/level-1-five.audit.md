<!-- ============================================================ -->
<!-- level-1-five.audit.md — AGENT ANNOTATIONS, NOT PAGE CONTENT.  -->
<!-- Build pipeline does NOT read this file (only level-*.md       -->
<!-- slugs listed in scripts/build-matrix.py LEVELS are rendered). -->
<!-- Audit scope: tillandsias runtime repo at v56.9.2.1 (tags,     -->
<!-- peeled; HEAD d6d3e3e). Run 2026-09-04. No page content was    -->
<!-- modified; these annotations exist for the next editing agent. -->
<!-- ============================================================ -->
Valid for pin v56.9.2.1 only; superseded by docs/audit/2026-09-05-v56.9.2.1.md once that record lands.

# Agent audit annotations — level 1 ("Like I'm 5")

Audited against `github.com/8007342/tillandsias` @ `v56.9.2.1`. Verdict: the
page is broadly accurate. One RED/PATH pair is now factually wrong and must be
re-sourced or downgraded before the next publish.

## Per-footnote verdicts

| Ref | Verdict | Note |
|-----|---------|------|
| [^1] | OK | app-lifecycle/spec.md#L12-L22 — "container/pod/image/runtime" ban is real. |
| [^2] | OK | enclave-network/spec.md#L10-L12 — single egress + stale-list story (six members) verifies. |
| [^3] | OK | philosophy.yaml#L63-L78 — destroy-and-recreate, no hand-patching. |
| [^4] | OK | math-foundations.yaml#L108-L120 — "not proven to end at zero" verifies. |
| [^5] | OK | philosophy.yaml#L7-L25 — many small bounded tries. |
| [^6] | OK | bringup_progress.rs#L119-L175 — banned-word machine check is real, incl. the test. |
| [^7] | **FALSE at pin** | The "doorman's key still broken / nobody has done it" framing is contradicted by the tag: packet 755-qcxh is CLOSED (plan/archive/packets-2026-08.yaml:15153, closed 2026-08-16); the CA private key travels as a podman secret (main.rs:3378-3382), is clamped/healed to 0600 (main.rs:3017-3020 + heal test main.rs:19231), and scripts/clamp-ca-material.sh migrates fleets. The cited issue file (plan/issues/proxy-ca-...-2026-08-15.md#L38-L46) itself still says "0644 / re-asserts 0644 on every pass" while the code is fixed — internally inconsistent source. |
| [^8] | OK (stale premise inside span) | tray-string-parity issue #L104-L130 — the badge word-list plan (628-w9sm) is genuinely open + operator-gated. BUT its cited span reasons from "TWO menu builders"; at the tag there is ONE builder (crates/tillandsias-host-shell/src/menu_state.rs — "the ONLY menu builder in the tree", order 628-p5tj). PATH claim survives. |
| [^9] | OK | action_host.rs#L255-L266 — "Ready (podman starting…)" and "VM failed" literals verify. Caveat: the "promise" banning these words is tray-ux/spec.md, not the [^1] app-lifecycle citation. |

## Recommended changes for the next editor

1. **RED/PATH [^7] — must fix.** Rewrite or downgrade. Ground truth at the pin:
   the private key still *exists* under /tmp generated by the binary, but exposure
   is closed (podman secret delivery, 0600 clamp+heal, fleet clamp script). Options:
   - Re-source to packet 755-qcxh + main.rs:3017-3020/3378-3382 + clamp-ca-material.sh
     and change the RED to a past-tense "was broken, now fixed in code" flag; or
   - If a residual RED is wanted, state it honestly: "a 0600 key file still lives
     under /tmp; the 0644 exposure is closed."
   - Do NOT keep "Nobody has done it" — it is false at v56.9.2.1.
   - The sentence "It is no longer unlocked (anyone could read it once; now only
     you can)" contradicts the issue file it cites; reconcile one source.

2. **[^8] premise** — if touching, point at the single-builder reality
   (menu_state.rs) while keeping the still-open badge word-list plan.

3. Everything else verifies; no line drift. Level 1 stays structurally sound.

## Ownership

This page (level 1) is owned for content by the website editors. These
annotations are advisory; the explanation text was intentionally not edited.