# Website re-verification — v56.9.21.1

- **Date:** 2026-09-22
- **Stable release:** `v56.9.21.1`, commit `2f5f2a90a7b43ecdee766e38561b8e5790c603aa`
- **Newest daily inspected for channel drift:** `v56.9.22.1`
- **Pins:** all five levels moved from `v56.9.12.2` to `v56.9.21.1`
- **Method:** source inspection and checked build against tagged checkouts. No live platform or public-deployment test was performed.
- **Gate:** `ok:issues:59 finding(s), 7 fragment(s)` and `ok:checked-build`; a second checked build at the same source state produced byte-identical HTML (SHA-256 `7d593c5d159dad0a0163a44f11d5edb7afa55b329f5d0728c5501c8adc13c6b7`).

The table counts all defined footnotes, including external references. The
re-verified column counts local quoted targets checked against a release. A moved target
is one whose cited range changed; an unchanged range with a still-valid quote is
not counted as moved. Callout counts track unchanged and rewritten GREEN, RED,
PATH, PROVEN, PLAUSIBLE and REFUTED statements; the evidence checks were made
against the stable checkout. The checked build verified quoted targets, while the
claim review established what those targets mean.

| Level | Footnotes at stable | Re-verified local quotes | Moved or corrected targets | Callouts retained | Callouts rewritten or removed | Pin |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Like I'm 5 | 26 | 26 | 6 | 7 | 0 | `v56.9.12.2` → `v56.9.21.1` |
| Phone | 37 | 36 | 7 existing; 3 added | 9 | 2 | `v56.9.12.2` → `v56.9.21.1` |
| Power user | 154 | 153 | 68 | 37 | 7 | `v56.9.12.2` → `v56.9.21.1` |
| Security | 104 | 103 | 38; 4 obsolete notes removed | 29 | 8 | `v56.9.12.2` → `v56.9.21.1` |
| MathWiz / Hacker | 45 | 33 | 8 | 18 | 0 | `v56.9.12.2` → `v56.9.21.1` |

The material flag changes are these: the Mac and Windows scripting-layer
portability check is now GREEN (`openspec/changes/expert-serve-grounded-pipeline/tasks.md#L52-L60`);
the inference engine download pins a version and digest
(`images/inference/entrypoint.sh#L329-L383`); the browser script defaults to the
enclave and accepts dotted project names
(`scripts/launch-chromium.sh#L93-L98`,
`crates/tillandsias-browser-mcp/src/allowlist.rs#L250-L257`); both sibling
network creators pass `--internal` and the shell gate discovers create sites
(`scripts/run-forge-project.sh#L127-L130`,
`scripts/diagnose-proxy.sh#L94-L97`,
`scripts/check-enclave-network-internal.sh#L130-L181`). The permissive proxy
port binds to loopback, and the mirror's credential helper checks the exact
host before returning a token (`images/proxy/squid.conf#L70-L78`,
`images/git/git-credential-tillandsias.sh#L58-L83`). The application reset
now refuses to reprovision when host credentials cannot be cleared, while the
separate Linux clean-room step still logs and continues on a failed clear or
probe (`crates/tillandsias-headless/src/main.rs#L9834-L9850`,
`scripts/e2e-step2-linux.sh#L30-L38`). These corrections preserve remaining
REDs for partial behavior rather than treating each landed fix as complete.

## Feature status at this stable release

| Capability the pages advertise | Status | Evidence at v56.9.21.1 |
| --- | --- | --- |
| Disposable forge and durable pushed work | implemented | `crates/tillandsias-headless/src/main.rs#L15906-L15909`; `images/git/pre-receive-hook.sh#L6-L8` |
| Stable install channel | implemented | Release tag returned by `/releases/latest`; site install shims resolve it at execution time. |
| Installer reset and reprovision | implemented | `scripts/install.sh#L313-L314`; `scripts/install-macos.sh#L240-L248`; `scripts/install-windows.ps1#L561-L566` |
| Local inference | partial | `images/inference/entrypoint.sh#L329-L383` pins and checks the engine; readiness remains best effort (`crates/tillandsias-headless/src/main.rs#L15301-L15308`). |
| Grounded local expert | partial | `openspec/changes/expert-serve-grounded-pipeline/tasks.md#L52-L60` records both portability lanes; the live-validator and synthesis limits remain in level 3. |
| Project web sibling | partial | The document-root, dotfile and lifecycle gaps remain in level 3, sourced to `plan/index.yaml#L51819` and the published launch path. |
| Host-browser tool | partial | `crates/tillandsias-browser-mcp/src/allowlist.rs#L250-L257` handles dotted names; `scripts/install-chromium.sh#L10-L16` still names no consumer. |
| Mac model-cache survival through a VM rebuild | partial | `crates/tillandsias-vm-layer/src/vz.rs#L874-L880` mounts the share; `plan/index.yaml#L31158` retains migration and end-to-end evidence work. |
| Stable requirement IDs and model property checks | partial | `scripts/check-requirement-ids.sh#L5-L35`; `crates/tillandsias-plan/src/obligation_props.rs#L172-L219`. The checked model is not every live validator. |
| Cross-release completion score | absent | Level 5 describes a bounded local score; `scripts/local-ci.sh#L612-L620` reports non-comparability after a broken regime. No complete application score was measured here. |

## Security status at this stable release

| Capability the pages advertise | Status | Evidence at v56.9.21.1 |
| --- | --- | --- |
| Enclave-only network creation | partial | Shell creators pass `--internal`; `scripts/check-enclave-network-internal.sh#L113-L181` states its Rust check remains file-scoped. |
| Proxy allowlist boundary | partial | `images/proxy/squid.conf#L70-L78` confines the permissive listener to loopback; the strict port's allowlist still admits anyone-can-publish namespaces. |
| Mirror credential isolation | partial | `images/git/git-credential-tillandsias.sh#L58-L83` checks host and protocol; the agent-facing push remains anonymous inside the enclave (`images/git/entrypoint.sh#L413-L420`). |
| Vault host-credential reset | partial | `crates/tillandsias-headless/src/main.rs#L9834-L9850` refuses incomplete app reset; `scripts/e2e-step2-linux.sh#L34-L38` treats a failed cold probe as best effort. |
| Persistent proxy CA location and key mode | partial | `crates/tillandsias-headless/src/main.rs#L3155-L3220` clamps the key but creates the parent directory without private-by-construction mode. |
| Hardened container launches | partial | Checked forge serialization is enforced; level 4 cites raw proxy, mirror, observatory and browser paths that do not all call the validator. |
| Linux SELinux confinement for the forge | absent | `openspec/specs/podman-orchestration/spec.md#L110` documents `label=disable`; MCS labelling is future work in `openspec/specs/default-image/spec.md#L768-L769`. |
| Encrypted control wire | partial | The secure setting defaults on, while level 4 records the explicit plaintext override and release-key limits. |
| Signed release artifacts | partial | `.github/workflows/release.yml#L500-L517` signs a fixed Mac asset list; Linux and Windows have broader transitive checks. |
| Signature verification during ordinary install | absent | `scripts/install.sh#L226-L239` checks a fetched checksum when available and may continue without it; no ordinary installer verifies a cosign bundle. |

## Other site surfaces and record

Home's install strip now says a current installer resets local application state
by default and names the opt-out. The Phone level gives the same warning in its
own words. Progress folds `issues.d/` and therefore now shows six older runtime
findings as resolved and one new Linux clean-room finding as found; it is a
findings ledger, not a measured application score. Slides, shared figures and
their captions were checked against the revised levels; no claim required a
deck or figure edit. The five-part order, navigation, slide count and deep-link
scheme were retained: 16 slides, no duplicate generated IDs, and every local
fragment either names an HTML ID or a level handled by the tab hash router.
The three install commands still use the site's rolling
stable shims. Level 5's eight citation-only deltas are listed for operator
review in `openspec/changes/level-5-v56-9-21-1/`.

No published GREEN was retracted in this run. Historical REDs that the stable
code has fixed were updated to describe the stable behavior, with the remaining
limits left visible. The six resolved ledger events carry release paths and
retain their original found events. The new runtime draft is
`2026-09-22-issues-tillandsias.md`; upstream filing was not performed.

## Verification limits

The checked build resolves every target and quote at its named checkout and
`scripts/issues.py validate` verifies ledger syntax and evidence. These checks
do not execute the runtime, prove a platform-specific installer succeeds,
confirm the public Cloudflare deployment, or independently verify external
scholarly sources in level 5. The daily channel was inspected for drift, but
its unpromoted fixes are not attributed to the stable channel.
