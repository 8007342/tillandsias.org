# Website re-verification — v56.9.21.1

- **Date:** 2026-09-24
- **Stable release:** `v56.9.21.1`, commit `2f5f2a90a7b43ecdee766e38561b8e5790c603aa`
- **Newest daily inspected:** `v56.9.23.1`, commit `29f7c2d`
- **Pins:** all five levels already pin stable `v56.9.21.1`; none moved.
- **Method:** the checked build opened every stable citation and quote. The daily comparison reviewed every cited file changed since that pin and the affected release-channel claims. Platform execution and public deployment were not tested.
- **Gate:** `ok:issues:61 finding(s), 9 fragment(s)` and two consecutive `ok:checked-build` results. Both builds wrote identical HTML, SHA-256 `235f1fd3def7d14e7993969374e3e1c65a70f48eb4807a816a2bdd7e24b8ce91`.

## Level audit

The stable target count is the complete footnote inventory. “Quoted at stable” counts local verbatim quotes checked by the build; unquoted external or long-range references are allowed. The changed-file count compares cited paths from the level's pin to the newest daily. Daily drift alone did not move a stable pin or invalidate a stable citation.

| Level | Footnotes | Quoted at stable | Stable targets corrected | Cited files changed by daily | Callouts retained / rewritten in prior stable audit | This run |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Like I'm 5 | 26 | 26 | 0 | 3 of 20 | 7 / 0 | Confirmed; no flag change |
| Phone | 37 | 37 | 1 citation corrected | 9 of 26 | 9 / 2 | Scoped Linux provenance claim and retracted the wider published wording |
| Power user | 154 | 153 | 0 | 9 of 61 | 37 / 7 | Confirmed; corrected the stale introductory pin text |
| Security | 104 | 103 | 0 | 9 of 51 | 29 / 8 | Confirmed; no stable flag change |
| MathWiz / Hacker | 45 | 33 | 0 | 2 of 15 | 18 / 0 | Confirmed; no text or pin change |

The prior stable review is [2026-09-22-v56.9.21.1](2026-09-22-v56.9.21.1.md). Stable source and pin are unchanged since that record. This run rechecked its claims against that same tagged tree, corrected the version sentence in level 3, and tightened level 2's Linux-specific provenance finding: the runtime record does not describe every platform, and the stable macOS tray embeds a build commit (`crates/tillandsias-macos-tray/src/main.rs#L58-L64`). The old universal sentence was withdrawn in a `retracted` ledger event. Its replacement links to the stable runtime finding and says only what that finding establishes.

The daily changed citations in every level. Among the reviewed changes, the new mirror `--sync` command, host-push identity and SSH certificate renewal work do not remove the documented anonymous-by-default lane: the SSH lane remains explicitly disabled unless `TILLANDSIAS_MIRROR_SSHD=1` (`crates/tillandsias-headless/src/main.rs#L11711-L11716` at the daily). No existing stable RED was therefore flipped on the strength of the daily. The daily-only work remains outside the stable page claims.

## Feature status at the stable release

The stable tag has not changed since the preceding feature inventory. These capability rows therefore carry forward from the prior record, whose citations resolve at this same tag.

| Capability advertised | Status | Evidence at `v56.9.21.1` |
| --- | --- | --- |
| Disposable forge and durable pushed work | implemented | `crates/tillandsias-headless/src/main.rs#L15906-L15909`; `images/git/pre-receive-hook.sh#L6-L8` |
| Stable install channel | implemented | `/releases/latest` and the site's rolling install shims |
| Installer reset and reprovision | implemented | `scripts/install.sh#L313-L314`; `scripts/install-macos.sh#L240-L248`; `scripts/install-windows.ps1#L561-L566` |
| Local inference | partial | `images/inference/entrypoint.sh#L329-L383`; readiness remains best effort at `crates/tillandsias-headless/src/main.rs#L15301-L15308` |
| Grounded local expert | partial | Portability runs are recorded in `openspec/changes/expert-serve-grounded-pipeline/tasks.md#L52-L60`; runtime validator and synthesis limits remain |
| Project web sibling | partial | Document-root, dotfile and lifecycle gaps remain in `plan/index.yaml#L51819` and the published launch path |
| Host-browser tool | partial | `crates/tillandsias-browser-mcp/src/allowlist.rs#L250-L257`; `scripts/install-chromium.sh#L10-L16` still names no consumer |
| Mac model-cache survival through VM rebuild | partial | `crates/tillandsias-vm-layer/src/vz.rs#L874-L880`; migration and end-to-end evidence remain open |
| Stable requirement IDs and model property checks | partial | `scripts/check-requirement-ids.sh#L5-L35`; `crates/tillandsias-plan/src/obligation_props.rs#L172-L219` |
| Cross-release completion score | absent | `scripts/local-ci.sh#L612-L620` reports non-comparability after a broken regime |

## Security status at the stable release

| Capability advertised | Status | Evidence at `v56.9.21.1` |
| --- | --- | --- |
| Enclave-only network creation | partial | `scripts/check-enclave-network-internal.sh#L113-L181`; its Rust check remains file-scoped |
| Proxy allowlist boundary | partial | `images/proxy/squid.conf#L70-L78`; strict-port allowlist still admits any enclave publisher |
| Mirror credential isolation | partial | `images/git/git-credential-tillandsias.sh#L58-L83`; agent-facing push remains anonymous at `images/git/entrypoint.sh#L413-L420` |
| Vault host-credential reset | partial | `crates/tillandsias-headless/src/main.rs#L9834-L9850`; Linux cold probe still continues on failure at `scripts/e2e-step2-linux.sh#L34-L38` |
| Persistent proxy CA and key mode | partial | `crates/tillandsias-headless/src/main.rs#L3155-L3220`; parent directory is not private by construction |
| Hardened container launches | partial | Checked forge serialization is enforced; level 4 documents raw paths that do not all use the validator |
| Linux SELinux confinement | absent | `openspec/specs/podman-orchestration/spec.md#L110` documents `label=disable` |
| Encrypted control wire | partial | Secure mode defaults on; explicit plaintext override and key limits remain |
| Signed release artifacts | partial | `.github/workflows/release.yml#L500-L517` signs a fixed Mac asset list; Linux and Windows checks differ |
| Signature verification during ordinary install | absent | `scripts/install.sh#L226-L239` may continue without a fetched checksum; ordinary install does not verify a cosign bundle |

## Other site surfaces and verification limits

Home positioning, plant facts, install commands and navigation were reviewed; the stable install commands remain rolling shims. Slides and figure captions were checked against the unchanged stable levels. The rendered deck contains 16 slides. Generated HTML has 918 unique IDs, no duplicate IDs, and all 366 local fragment links resolve (the tab-router's level and view fragments are handled separately).

Progress remains a build-time findings/spec/checklist ledger and makes no measured completion-score claim. The generated Progress and Big Graph links now point at this audit. The checkout helper previously omitted historical runtime tags referenced only by ledger evidence; this made a fresh checked build report 67 quotes as unchecked. It now adds those tags, and a no-argument fetch prepared `v56.9.5.1` automatically. No runtime defect was newly filed or relayed upstream in this run.

The second checked build at the same source state was byte-identical to the first. These checks establish citation integrity and generated-page structure. They do not execute Tillandsias, test platform-specific installation, independently validate level 5's external mathematical sources, or confirm the public Cloudflare deployment.
